version: '3.8'

services:
  ffc-grants-frontend:
    command: npm run start:watch
    build:
      target: development
    image: ffc-grants-frontend-development
    environment:
      NODE_ENV: test
    volumes:
      - ./test-output/:/home/node/test-output/
