version: '3.8'

services:
  ffc-grants-frontend:
    build:
      context: .
      target: production
    image: ffc-grants-frontend
    environment:
      NODE_ENV: production
      SITE_URL: localhost:3000
